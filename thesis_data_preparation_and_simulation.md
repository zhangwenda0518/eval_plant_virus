# 第X章 基准评估体系的构建：数据准备与模拟方法

## X.1 引言

构建一个科学、公正、可重复的基准评估体系是评估宏病毒组分析工具性能的前提。本章系统阐述植物宏病毒组基准评估体系的数据准备与模拟方法，涵盖参考数据库构建、评估用病毒选取策略、模拟数据生成引擎、以及面向五项评估任务的标准化数据集构建流程。

基准评估的核心挑战在于：如何生成既反映真实生物学特征、又具备已知绝对真值（ground truth）的测试数据。本研究采用"模拟+掺入"（simulation + spike-in）策略，以真实基因组为模板，通过物理测序模拟生成已知组成的宏病毒组数据集，从而为下游工具的灵敏度、特异性、定量准确性和组装完整性评估提供可控的实验条件。

---

## X.2 材料与方法

### X.2.1 植物病毒参考数据库的构建

本研究构建的植物病毒参考数据库（`final.cluster.ref_info.tsv`和`final.cluster.ref.fasta`）是后续所有评估工作的数据基础。该数据库整合了NCBI GenBank和RefSeq的植物病毒基因组序列，经过去冗余聚类（CD-HIT-EST，95%核酸相似度阈值）和ICTV分类信息标准化处理，共收录X个病毒分类单元（cluster）对应的Y条参考序列。每条记录包含以下关键注释字段：

- **分类信息**：ICTV科（VMR_Family）、属（VMR_Genus）、种（VMR_Species）三级分类
- **基因组特征**：核酸完整性（Nuc_Completeness）、拓扑结构（Topology）、分子类型（Molecule_type）、基因组长度（Length）
- **序列来源**：类别（Category，如NonSegmented/Segmented_Complete）、序列类型（Sequence_Type，如ICTV/RefSeq/GenBank）
- **宿主信息**：宿主物种（Host）、GenBank标题（GenBank_Title）

---

### X.2.2 评估用病毒的选取策略

评估的基础是选取具有充分代表性的病毒基因组。本研究开发的`prep_select_eval_viruses.py`脚本从构建的植物病毒参考数据库中进行分层随机选取。

#### X.2.2.1 选取约束条件

为确保评估的科学性和可解释性，对候选病毒施加以下硬约束：

1. **基因组完整性**：仅选取`Nuc_Completeness`为"complete"的完整基因组，排除片段化的参考序列。
2. **分类信息完整性**：要求`VMR_Family`（科）、`VMR_Genus`（属）和`VMR_Species`（种）三级ICTV分类信息均非空。
3. **非植物宿主排除**：通过匹配关键词（chlorella、phycodnaviridae、chlorovirus、prasinovirus、coccolithovirus、prymnesiovirus、phaeovirus、raphidovirus）从`Host`和`GenBank_Title`字段中排除绿藻病毒等非植物宿主病毒。
4. **非节段性**：对于非节段病毒评估（评估1-4的主体部分），排除`Category`为"Segmented"的病毒基因组，保留"NonSegmented"和未明确标记非节段的序列。

#### X.2.2.2 分层配额策略

为保证评估覆盖植物病毒的主要基因组类型多样性，按基因组类型（基于Topology、Molecule_type、Molecule_Type2、Length和GenBank_Title综合判定）进行分层选取。基因组类型分类逻辑如下：

| 判定优先级 | 条件 | 类型 |
|---|---|---|
| 1 | Length ≤ 500 bp 且标题含"viroid" | viroid |
| 2 | Molecule_Type2含"dsdna"和"rt" | dsDNA-RT |
| 3 | Molecule_type = "ssDNA" | ssDNA |
| 4 | Molecule_type = "dsRNA" | dsRNA |
| 5 | Molecule_type = "ssRNA(-)" | ssRNA(-) |
| 6 | Molecule_type = "ssRNA(+)"（兜底） | ssRNA(+) |

选取配额按表X.1分配，确保各基因组类型均获得充分代表。

**表X.1 非节段病毒分层选取配额**
| 基因组类型 | n=50（完整评估） | n=30（精简评估） | 代表特征 |
|---|---|---|---|
| ssRNA(+) | 15 | 10 | 最大植物病毒类群，如Potyviridae |
| ssRNA(-) | 8 | 4 | 如Rhabdoviridae、Bunyavirales |
| dsRNA | 6 | 4 | 如Partitiviridae、Endornaviridae |
| ssDNA | 10 | 6 | 环状ssDNA，如Geminiviridae |
| dsDNA-RT | 5 | 3 | 逆转录病毒特征，如Caulimoviridae |
| viroid | 6 | 3 | 类病毒，长度<500 bp，非编码RNA |

在每个基因组类型内部，进一步按长度分箱（ultra_short: ≤500 bp; short: 501-2,000 bp; medium: 2,001-6,000 bp; long: 6,001-15,000 bp; ultra_long: >15,000 bp）确保长度分布的代表性。选取分两轮进行：第一轮从每个有候选序列的分箱中各选1个代表；第二轮从剩余候选中随机补齐配额。配额不足时，从其他候选随机补充。

#### X.2.2.3 节段病毒选取

对于评估2和评估3中需要节段病毒的场景，额外从ICTV Species中选取完整的分节段物种。选取逻辑如下：

1. 从参考数据库中筛选`Sequence_Type`包含"ICTV"且`Category`为"Segmented_Complete"的记录。
2. 按`Taxid`分组，同一`Taxid`的不同accession视为该物种的所有节段。
3. 排除类病毒科（Pospiviroidae、Avsunviroidae）。
4. 按节段数分三个分箱：少节段（2-3段）、中节段（4-7段）、多节段（8+段），从各箱等量选取。
5. 去重：同一物种名仅选取一次。

`--include`参数支持强制包含指定的病毒accession（如已知的重要植物病毒），`--seed 42`确保选取的可重复性。选取结果输出为单个FASTA文件（每个病毒一个文件）和`selected_viruses.tsv`记录表。

---

### X.2.3 模拟数据生成引擎：virome_simulator.py

模拟数据的生成是整个基准评估体系的基础。本研究开发的`virome_simulator.py`是一个全功能的宏病毒组模拟数据生成引擎，整合了突变模拟、丰度配置、测序模拟、宿主掺入和深度梯度子采样五项核心功能。该引擎通过子命令（subcommand）架构实现模块化调用，支持断点续传（`--resume`）、多线程并行和系统资源监控。

#### X.2.3.1 长度感知的摩尔丰度分配

传统的模拟数据生成方法通常对所有病毒分配相等的测序reads数量，忽略了病毒基因组长度对测序物理过程的影响。根据测序仪的物理原理，在相同的摩尔浓度下，更长的基因组会产出更多的测序reads（因为每个基因组分子可以提供更多的片段化起始点）。本研究采用以下长度感知的丰度分配策略：

**算法X.1 长度感知摩尔丰度分配**

设评估用病毒基因组集合为 $\mathcal{G} = \{G_1, G_2, \ldots, G_n\}$，目标总reads数为 $R_{\text{total}}$。

**步骤1**：为每个病毒基因组独立采样对数正态分布的摩尔丰度值，模拟自然界中病毒丰度的典型分布特征：
$$a_i \sim \text{Lognormal}(\mu=0, \sigma=1.5)$$

**步骤2**：计算每个病毒的测序权重 $w_i = a_i \times L_i$，其中 $L_i = |G_i|$ 为病毒基因组长度（bp）。该权重反映了测序产出与摩尔丰度和基因组长度的乘积成正比这一物理事实。

**步骤3**：对所有病毒归一化权重：
$$p_i = \frac{w_i}{\sum_{j=1}^{n} w_j}$$

**步骤4**：分配reads数：
$$r_i = \lfloor R_{\text{total}} \times p_i \rfloor$$
最后一个病毒获得剩余reads：$r_n = R_{\text{total}} - \sum_{i=1}^{n-1} r_i$。所有reads数强制调整为偶数，以适配双端测序的最低要求。

该策略在物理意义上正确地模拟了真实测序仪的产出过程，为下游的Spearman秩相关评估（实际丰度与预测丰度的相关性）提供了科学基础。

在均匀深度模式下（`--depth`参数），每个病毒的reads数按目标测序深度直接计算：
$$r_i = \lceil \frac{D \times L_i}{l_{\text{read}}} \rceil$$
其中 $D$ 为指定的测序深度倍数，$l_{\text{read}}$ 为reads长度。这适用于组装评估等需要控制单一变量的场景。

#### X.2.3.2 测序模拟与质量控制

测序模拟调用ART（v2.5+）模拟Illumina测序的完整物理过程。ART是经广泛验证的经验性测序模拟器，其关键参数配置如下：

- **错误模型**：HiSeq 2500经验错误谱（`-ss HS25`），包含碱基替换错误、插入/缺失错误和质量分数分布
- **片段化模拟**：高斯分布建模片段长度（均值300 bp，标准差50 bp）（`-m 300 -s 50`）
- **双端测序**：150 bp配对端模式（`-l 150 -p`）
- **GC偏差**：ART内置的GC含量偏差校正

模拟采用跨基因组的多线程并发策略：每个病毒基因组的ART调用为独立任务，通过`ThreadPoolExecutor`并行执行，不同任务使用不同的ART随机种子（$\text{seed}_k = (\text{global\_seed} + k \times 777) \mod 2^{31}-1$），避免跨基因组的重复随机序列产生。

模拟完成后执行以下质量控制步骤：

1. **reads合并**：将所有病毒基因组产出的R1和R2文件分别进行二进制级联合并。
2. **随机重排**：使用SeqKit shuffle对合并后的reads进行随机重排（`seqkit shuffle -s SEED`），消除基因组模拟顺序带来的系统偏差。
3. **配对修复**：使用BBTools `repair.sh`进行配对端reads的配对修复，确保R1和R2文件中reads的顺序严格一致，支持下游工具的成对分析。

#### X.2.3.3 突变模拟

为评估已知病毒检测工具对序列突变的容忍度，引擎集成了突变模拟功能（`mutate`子命令）。突变模拟调用`mutation-simulator`工具，参数设置为：

- 单核苷酸多态性（SNP）率：`-sn rate/100`（如5%突变率对应`-sn 0.05`）
- 插入/缺失（Indel）率：`-in 0.001`（固定0.1%）

由于`mutation-simulator`存在已知的输出路径bug，引擎实现了"替身机制"绕过该问题：先将输入基因组拷贝到输出目录内的临时文件，运行突变模拟后通过glob模式匹配抓取输出文件，再重命名为标准命名格式（`{accession}_mut_{rate}pct.fa`）。

本研究设置了4个突变率梯度：5%、10%、15%和30%，覆盖从低度（近缘病毒株间）到高度（远缘病毒属间）的序列变异范围。

#### X.2.3.4 宿主背景掺入策略（LoD测试）

模拟宏病毒组的核心挑战是真实反映临床/环境样本中病毒核酸占比极低的特征。本研究实现了基于测序深度的精确spike-in策略（`LoD_mix`子命令）：

**标准模式**：对每个靶标病毒在每个深度梯度下生成独立样本，每个样本混合固定reads数的宿主背景（`--bg-reads`）和按深度计算的病毒reads：
$$r_{\text{virus}} = \lceil \frac{d \times L_{\text{virus}}}{l_{\text{read}}} \rceil$$

其中 $d$ 为靶标深度（如0.5×、1.0×、...、1000×）。

**混合模式（`--all-in-one`）**：将所有靶标病毒混合到同一个样本中，每个病毒按统一的靶标深度分配reads。同时生成真值表（`SpikeIn_GroundTruth.tsv`）和配置文件（`config.txt`），记录每个病毒的真实reads数、占比和深度信息。

LoD测试默认设置10个深度梯度（0.5×、1×、5×、10×、20×、50×、100×、200×、500×、1000×），覆盖从极低丰度（~0.0003%）到明确可检（~5%）的完整动态范围。

---

### X.2.4 评估一：宿主过滤评估数据集的构建

评估一旨在评估宏病毒组分析流程中宿主reads去除步骤的效率和假阳性率。数据集构建采用`LoD_mix`子命令的`--all-in-one`模式：

1. 使用50个非节段评估用病毒的全长基因组作为靶标。
2. 以宁夏枸杞（*Lycium barbarum*）基因组（`ningxia.genome.fasta`）为宿主背景参考。
3. 生成10M reads的共享宿主背景。
4. 将50个病毒以1000×深度掺入（每个病毒的reads数 = $\lceil 1000 \times L_{\text{virus}} / 150 \rceil$）。
5. 合并宿主与病毒reads后随机重排和配对修复。

该设计模拟了典型的植物宏病毒组样本——大量宿主reads背景中混杂微量的病毒reads，真实反映了宿主序列去除的挑战。

---

### X.2.5 评估二：已知病毒检测评估数据集的构建

评估二分为两个子评估：丰度定量评估（2A）和突变容忍评估（2B）。

#### X.2.5.1 评估二A：丰度定量评估

评估二A旨在评估已知病毒检测工具对病毒丰度的定量准确性。数据集构建采用长度感知摩尔丰度分配模式：

1. 以50个评估用病毒为模板。
2. 使用`gen-config`子命令生成丰度配置文件，设定总reads数为5M，各病毒的reads按长度感知对数正态分配。
3. 使用`simulator`子命令执行双端测序模拟。

该设计使各病毒的reads产出存在固有的丰度差异，反映自然病毒群落的丰度分布特征，可用于评估工具对相对丰度的排序能力（Spearman秩相关）和绝对定量的准确性（Pearson相关）。

#### X.2.5.2 评估二B：突变容忍评估

评估二B旨在评估已知序列比对工具对病毒基因组突变的容忍能力。数据集构建流程：

1. **突变生成**：对50个评估用病毒的全长基因组，分别以4个突变率（5%、10%、15%、30%）生成突变版本。
2. **配置文件生成**：对每个突变率的突变基因组集合，生成均匀深度1000×的测序配置文件。
3. **测序模拟**：对每个突变率的配置执行双端测序模拟。

该设计控制丰度为均匀分布（1000×），以使突变率成为唯一的自变量，从而评估不同序列变异程度对检测灵敏度和覆盖度的影响。

金标准文件（`eval_known_virus_gold.tsv`和`eval_merged_input.tsv`）通过`prepare_cami_input.py`脚本生成，记录每个病毒在每种条件下的预期reads占比。

---

### X.2.6 评估三：组装评估数据集的构建

评估三旨在评估宏病毒组组装工具在不同测序深度下的组装性能。数据集构建覆盖8个深度梯度：

1. 以50个非节段病毒和额外选取的节段病毒（`--n-segmented 10`）的全长基因组为模板。
2. 对每个深度梯度（10×、20×、50×、100×、200×、500×、1000×、2000×）独立生成配置文件（均匀深度模式）。
3. 对每个深度梯度执行双端测序模拟。

该设计覆盖了从极低深度（组装困难）到高深度（接近理想组装）的完整梯度，可系统评估深度对组装连续性（N50、contig数量）和完整性（基因组回收率）的影响。

---

### X.2.7 评估四：病毒鉴定评估数据集的构建

评估四旨在评估病毒鉴定工具区分病毒序列与非病毒序列的能力（二元分类问题）。数据集构建使用`prep_build_id_eval_seqs.py`脚本，包含以下四类序列：

#### X.2.7.1 正样本（Positive）：病毒基因组覆盖率梯度片段

从评估用病毒基因组中，按7个覆盖率梯度（100%、90%、80%、70%、60%、50%、40%）截取片段：

- 片段长度 = $\max(200, L_{\text{full}} \times \text{cov\_pct} / 100)$
- 随机起始位置（模拟组装断点位置的不确定性）
- 每个病毒在每个覆盖率下截取2个复制（`--n-per-coverage 2`）
- 最小片段长度200 bp，确保评价标准的生物学合理性

#### X.2.7.2 负样本A：宿主基因组随机片段

从宿主参考基因组（宁夏枸杞）中随机截取500条片段，长度分布与正样本的片段长度分布相匹配（长度匹配 ±200 bp范围内随机扰动），模拟宿主DNA污染产生的假阳性挑战。

#### X.2.7.3 负样本B：保守结构域陷阱序列

这是本研究最具特色的负样本类型。选取含病毒保守蛋白结构域（Pfam结构域）的宿主蛋白编码序列，模拟因序列同源性导致的假阳性。目标Pfam结构域经文献调研和实际假阳性分析确定（表X.2）。

**表X.2 病毒鉴定评估中的保守结构域陷阱**
| Pfam ID | 结构域名称 | 假阳性风险 |
|---|---|---|
| PF00270 | DEAD_helicase | RNA解旋酶，在病毒和宿主中均高度保守 |
| PF00271 | DEAH_helicase | RNA解旋酶变体 |
| PF00069 | Protein_kinase | 蛋白激酶，在病毒-宿主互作中保守 |
| PF00096 | Zinc_finger_C2H2 | 锌指DNA结合结构域 |
| PF13912 | Zinc_finger_C2H2_type | 锌指结构域变体 |
| PF00437 | NTPase_T2SS | NTPase结构域 |
| PF00680 | RdRp_1 | RNA依赖RNA聚合酶（病毒标志） |
| PF00978 | RdRp_2 | RNA依赖RNA聚合酶变体 |

陷阱序列的提取流程包括两个阶段：

**阶段一（Pfam注释）**：使用`para_hmmscan.pl`（HMMER3并行封装脚本）对宿主蛋白序列进行Pfam-A v35结构域注释。参数设置：序列E-value阈值1e-5，结构域E-value阈值1e-3，最小HMM覆盖率25%。输出简化的7列格式（GeneID、HMM_accession、HMM_Name、E-value、Score、Coverage、Description）。

**阶段二（序列提取与筛选）**：
1. 通过GFF3注释文件使用`gffread`提取含目标Pfam结构域的蛋白对应的CDS编码核酸序列（优先方案）；若无GFF3文件，降级使用蛋白FASTA序列。
2. 最小长度过滤：保留≥500 bp的CDS序列。
3. 去重：同一序列命中多个Pfam结构域时仅计为一条。
4. 按Pfam类型分层选取：每个Pfam类型选取等量序列，总计300条（`--n-sequences 300`）。
5. 截取500-3,000 bp的随机片段，模拟组装碎片化的contig长度分布。

#### X.2.7.4 负样本C：EVE/转座子序列

内源性病毒元件（Endogenous Viral Elements, EVEs）是整合到宿主基因组中的病毒序列遗迹，在宏病毒组分析中容易造成假阳性。负样本C从C-RVDB数据库的去病毒序列（`C-RVDBvCurrent.PLN.not-virus.fasta`）中截取200条片段，长度与正样本匹配。若无专用EVE序列文件，降级使用宿主基因组中≥5,000 bp的长序列。

#### X.2.7.5 数据集统计汇总

四条序列集合并后生成统一的评估文件：`evaluation_sequences.fasta`（合并FASTA）和`sequence_labels.tsv`（真值标签）。典型的数据集规模为：50个病毒 × 7覆盖率 × 2复制 = 700条正样本 + 500条负样本A + 300条负样本B + 200条负样本C = 1,700条总序列。

---

### X.2.8 评估五：分类评估数据集的构建

评估五旨在评估病毒分类工具在科/属/种层级的分类准确性。数据集构建使用`prep_build_class_eval_seqs.py`脚本，核心特点为防信息泄漏（leakage prevention）设计。

#### X.2.8.1 测试序列生成

从评估用病毒基因组中按覆盖率梯度（100%、90%、80%、70%、60%、50%、40%）截取测试片段：

- 最小片段长度500 bp（分类任务需要更多序列信息，高于鉴定评估的200 bp阈值）
- 每个病毒在每个覆盖率下截取2个复制
- 记录元数据：源accession、ICTV科/属/种、覆盖率、完整长度、片段长度

#### X.2.8.2 防泄漏参考数据库构建

传统的分类评估直接将测试序列从参考数据库中比对，存在信息泄漏风险——如果测试序列本身或其近缘序列存在于参考数据库中，分类器可能通过"记忆"而非"推断"获得正确答案。本研究实现了严格的防泄漏策略：

1. **精确匹配排除**：测试序列的源accession从参考数据库中完全移除。
2. **同种排除**：与测试序列属于同一ICTV物种的所有序列一并移除（防止同种不同株间的序列相似性导致的信息泄漏）。

输出包括去泄漏参考数据库（`db_sequences.fasta`）、合并测试序列文件（`test_sequences_merged.fasta`）和测试元数据表（`test_metadata.tsv`）。

#### X.2.8.3 分类元数据富集

使用`enrich_metadata_taxonomy.py`脚本通过NCBI Entrez API为测试序列补充完整的NCBI分类学谱系信息，输出`test_metadata_full.tsv`，以支持下游对不同分类层级（界/门/纲/目/科/属/种）的评估。

---

## X.3 结果

### X.3.1 病毒选取结果

从包含2,263条候选非节段序列的参考数据库中，经分层选取获得50个代表性病毒基因组。排除统计如下：节段病毒5,197条、非完整基因组2,743条、ICTV分类不完整1,743条、非植物宿主10条。

选取结果的基因组类型分布见表X.3。

**表X.3 50个评估用病毒的基因组类型分布**
| 基因组类型 | 选取数量 | 配额 | 候选池 |
|---|---|---|---|
| ssRNA(+) | 14 | 15 | 1,082 |
| ssRNA(-) | 8 | 8 | 120 |
| dsRNA | 6 | 6 | 35 |
| ssDNA | 9 | 10 | 757 |
| dsDNA-RT | 4 | 4 | 131 |
| viroid | 4 | 4 | 136 |
| 补充填充 | 3 | — | — |
| **总计** | **50** | **50** | **2,263** |

注：部分基因组类型（ssRNA(+)、ssDNA、viroid）未能完全配满配额，由其他候选随机补充。这主要由于ICTV分类完整+非节段+完整基因组的联合约束对某些类型的限制较严格。

病毒基因组的拓扑结构分布为31条线性（linear）和18条环状（circular），1条未记录（Not_Found），反映了植物病毒基因组的真实结构多样性。长度范围从330 bp（类病毒）到44,539 bp（大RNA病毒），覆盖两个数量级。选取的病毒分布于22个科和29个属，确保了分类多样性的充分覆盖。

此外，从83个多节段ICTV物种中共选取了6个节段病毒物种的46条accession，包括Fijivirus（9节段）、Emaravirus（5-6节段）、Tenuivirus（5节段）、Olpivirus（4节段）、Begomovirus（2节段）等代表性类群。

### X.3.2 模拟数据集生成概况

模拟数据生成流程共产生以下评估数据集（表X.4）：

**表X.4 评估数据集生成概况**
| 评估任务 | 数据集目录 | 样本配置 | 关键参数 |
|---|---|---|---|
| 评估一（宿主过滤） | `eval1_host_dep` | 1个混合样本 | 50病毒混合、10M宿主reads、1000×深度 |
| 评估二A（丰度定量） | `eval2a_abundance` | 1个样本 | 5M总reads、对数正态丰度分配 |
| 评估二B（突变容忍） | `eval2b_mut_{0/5/10/15/30}` | 5个样本（4个突变+1原始） | 1000×均匀深度、4突变率 |
| 评估三（组装） | `eval3_depth{10-2000}` | 8个样本 | 8深度梯度、50+10节段病毒 |
| 评估四（鉴定） | `step3_identification_eval` | 1个合并文件 | 700正 + 500负A + 300负B + 200负C |
| 评估五（分类） | `step4_classification_eval` | 1个测试集+去泄漏数据库 | 覆盖率梯度、防泄漏设计 |

### X.3.3 保守结构域陷阱序列的特征

从宁夏枸杞宿主蛋白组的Pfam注释结果中，筛选获得了含8个目标病毒保守结构域的宿主编码序列。按Pfam类型分层选取300条陷阱序列，用于评估四的负样本B。该设计直接回应了前期实验观察——含有DEAD/DEAH解旋酶、蛋白激酶和RdRp远缘同源结构域的宿主蛋白是最常见的Diamond BLASTx假阳性来源。

---

## X.4 讨论

### X.4.1 分层选取策略的优势与局限

本研究采用的分层选取策略（按基因组类型配额+长度分箱）确保了评估病毒在生物学多样性维度上的代表性，优于简单的随机选取。然而，由于ICTV分类完整性和基因组完整性的联合约束，ssRNA(+)和ssDNA类型未能完全配满配额。这一局限源于当前公共数据库中部分序列的分类注释不完整，而非方法本身的设计缺陷。随着ICTV分类体系的持续更新和数据库注释质量的提升，该问题将逐步缓解。

### X.4.2 长度感知丰度分配的物理学基础

长度感知摩尔丰度分配是本研究的一个重要方法论贡献。传统的等reads数分配方法隐含假设所有病毒具有相同的基因组长度——这在病毒基因组长度跨越两个数量级（~300 bp到~45 kb）的情况下显然不成立。本研究的分配策略基于测序化学的基本原理：在等摩尔浓度下，长基因组产生更多的片段化起始点，因而产出更多的测序reads。对数正态分布（$\mu=0, \sigma=1.5$）的引入进一步模拟了自然界病毒群落丰度的典型分布。该设计为评估工具定量准确性的Spearman秩相关提供了物理上正确的参照系。

### X.4.3 陷阱序列设计的创新性

病毒鉴定评估中负样本B的设计是本研究的一个重要创新。传统的阴性对照通常仅包括宿主基因组随机片段，忽略了一个关键问题：与病毒蛋白共享保守结构域的宿主蛋白序列是宏病毒组分析中假阳性的重要来源。通过系统性地将Pfam结构域注释与宿主蛋白组分析相结合，并针对8个病毒保守结构域提取宿主CDS序列，本研究的鉴定评估提供了一种对序列比对工具（尤其是基于氨基酸水平的Diamond BLASTx）特异性进行压力测试的有效手段。

### X.4.4 防泄漏设计对分类评估的重要性

分类评估中的防泄漏数据库构建是确保评估公正性的关键。如果不排除同种序列，分类工具可能通过简单的序列相似性匹配获得正确的分类结果——这在真实场景中对应于"该病毒物种已在数据库中"的情况，而非分类算法的泛化能力。本研究的两级排除策略（精确匹配+同种排除）强制分类工具依赖于跨物种的序列特征推断，更真实地反映了对新发现病毒的分类能力。

### X.4.5 模拟数据的生态效度

任何模拟数据均与真实样本存在差距，本研究设计的模拟数据集也不例外。主要简化包括：（1）仅使用完整基因组作为模板，忽略了真实样本中存在的片段化病毒核酸；（2）使用统一的HiSeq 2500错误模型，未考虑不同测序平台间的差异；（3）突变模拟仅考虑点突变和小indel，未模拟重组和重排等复杂变异。这些简化是实验控制与生态效度之间的必要权衡——精确控制变量是评估工具性能的前提条件。未来研究可结合真实模拟测序（如 Nanopore 的长reads模拟）和实际标准样本（如mock community）进一步验证。

---

## X.5 本章小结

本章系统阐述了植物宏病毒组基准评估体系的数据准备与模拟方法，涵盖以下核心内容：

1. **病毒选取**：基于ICTV分类的6类基因组类型分层配额策略，从2,263条候选序列中选取50个非节段病毒和6个节段病毒物种（46条accession），确保基因组长度的跨量级覆盖和分类多样性的充分代表性。

2. **模拟引擎**：开发了全功能宏病毒组模拟数据生成引擎`virome_simulator.py`，整合长度感知摩尔丰度分配、ART物理测序模拟、突变生成和宿主背景掺入等核心功能。

3. **五项评估数据集**：构建了面向宿主过滤（评估一）、已知病毒检测（评估二A/B）、组装（评估三）、鉴定（评估四）和分类（评估五）的标准化评估数据集，每一数据集均配有绝对真值标签。

4. **方法创新**：提出了长度感知摩尔丰度分配策略、保守结构域陷阱序列设计和防泄漏参考数据库构建等三项方法论创新，提升了基准评估的物理真实性和科学严谨性。
